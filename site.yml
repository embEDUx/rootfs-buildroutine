---
- hosts: localhost
  roles:
    - packages_prepare
    - qemu_stop
    - image_setup
    - qemu_prepare
    - qemu_run

- include: cross.yml
  when: native_arch != target_arch

- hosts: qemu
  roles:
    - packages_flags
    - packages_install

- hosts: localhost
  roles:
    - qemu_stop
    - rootfs_copyoverlay
    - rootfs_pack
